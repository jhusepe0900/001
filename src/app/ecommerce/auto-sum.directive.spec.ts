import { AutoSumDirective } from './auto-sum.directive';

describe('AutoSumDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoSumDirective();
    expect(directive).toBeTruthy();
  });
});
